name: Create Docker image on push to dev, main, or releases

on:
  push:
    branches:
      - "dev"
      - "main"
    tags:
      - "v*.*.*"
  release:
    types: [published]
  workflow_dispatch:

# Image builds are slow; a rapid series of pushes to main should not queue a
# build per commit when only the last one will be tagged :main anyway.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    name: Build and Push Docker image to DockerHub
    runs-on: ubuntu-latest
    # Pushing to ghcr.io needs packages:write. This worked only because the repo
    # still grants the default token read/write; it breaks silently the moment
    # default workflow permissions are tightened to read-only.
    permissions:
      contents: read
      packages: write
    steps:
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            isaacson21/achilles
            ghcr.io/AchillesGen/Achilles
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
            
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Build and push to DockerHub
        uses: docker/build-push-action@v6
        with:
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
